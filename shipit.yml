ci:
  require:
    - continuous-integration/jenkins/pr-merge
  hide:
    - continuous-integration/jenkins/branch

merge:
  require:
    - continuous-integration/jenkins/pr-merge
  ignore:
    - continuous-integration/jenkins/branch

review:
  checks:
    - echo "im review.checks"

deploy:
  override:
    - ls -la script && ./deploy.sh


