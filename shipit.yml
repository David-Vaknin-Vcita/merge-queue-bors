ci:
  require:
    - continuous-integration/jenkins/pr-merge
  hide:
    - continuous-integration/jenkins/branch

merge:
  require:
    - continuous-integration/jenkins/pr-merge
    - echo "im megre.require"
  ignore:
    - continuous-integration/jenkins/branch

review:
  checks:
    - echo "om review.checks"

deploy:
  override:
    - ./script/deploy.sh


