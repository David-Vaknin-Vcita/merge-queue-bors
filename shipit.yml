ci:
  require:
    - continuous-integration/jenkins/pr-merge
  hide:
    - continuous-integration/jenkins/branch
merge:
  require:
    - continuous-integration/jenkins/pr-merge
  ignore:
    - continuous-integration/jenkins/branch

review:
  checks:
    - echo "will fail" && exit(1)
deploy:
  override:
    - pwd && ls -la


